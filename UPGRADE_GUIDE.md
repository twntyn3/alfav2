# Руководство по улучшению системы

## 🎯 Ключевые файлы для апгрейда нейронных сетей

### 🔥 Приоритет 1: Максимальный эффект на качество

#### 1. `src/retriever.py` - Hybrid Retriever (Embeddings)
**Текущая модель**: `intfloat/multilingual-e5-large`  
**Возможные улучшения**:
- ✅ **BGE-M3** (`BAAI/bge-m3`) - самая мощная multilingual embedding модель
  - 560M параметров, лучше чем e5-large для русского
  - Поддержка dense + sparse + multi-vector retrieval
  - Ожидаемый прирост: +3-5% Hit@5
  
- ✅ **Cohere multilingual-v3** - коммерческая, но очень качественная
  - Лучшее качество для финансовых текстов
  - Ожидаемый прирост: +5-7% Hit@5

- ✅ **Jina-embeddings-v2** (`jinaai/jina-embeddings-v2-base-en/de/zh`) - хорошая альтернатива
  - 137M параметров, быстрая и качественная
  
**Файл для модификации**: `src/retriever.py` (строки 30-45, где создается HybridRetriever)

---

#### 2. `src/reranker.py` - Cross-Encoder Reranker
**Текущая модель**: `BAAI/bge-reranker-base`  
**Возможные улучшения**:
- ✅ **BGE-Reranker-v2** (`BAAI/bge-reranker-v2-m3`) - новая версия с улучшенным качеством
  - Ожидаемый прирост: +2-4% Hit@5
  - Поддержка до 8192 токенов (против 512)

- ✅ **Cohere rerank-v3** - коммерческая, но очень точная
  - Лучшее качество на русском языке
  - Ожидаемый прирост: +4-6% Hit@5

- ✅ **Multilingual-E5-Base-Instruct** (`intfloat/multilingual-e5-base-instruct`)
  - Специально обучена для reranking
  - Ожидаемый прирост: +1-3% Hit@5

**Файл для модификации**: `src/reranker.py` (строки 23-48, где создается Reranker)

---

#### 3. `src/reranker_ensemble.py` - Ensemble Reranker
**Текущая конфигурация**: Отключен (`enabled: false`)  
**Возможные улучшения**:
- ✅ **Включить ensemble** с двумя сильными моделями:
  - `BAAI/bge-reranker-v2-m3` (вес 0.6)
  - `BAAI/bge-reranker-base` (вес 0.4)
  - Ожидаемый прирост: +3-5% Hit@5

- ✅ **Второй проход reranking** на топ-20 кандидатов
  - Ожидаемый прирост: +1-2% Hit@5

**Файл для модификации**: `configs/models.yaml` (строки 24-31)

---

### 🚀 Приоритет 2: Дополнительные улучшения

#### 4. `src/semantic_chunker.py` - Semantic Chunking
**Текущий подход**: Правила + TokenChunker  
**Возможные улучшения**:
- ✅ Использовать **embedding-based semantic chunking**
  - Использовать embedding модель для определения семантических границ
  - Ожидаемый прирост: +2-3% Recall@K

- ✅ **Hierarchical chunking** с разными размерами
  - Большие chunks (500 токенов) для общих запросов
  - Маленькие chunks (200 токенов) для конкретных фактов
  - Ожидаемый прирост: +1-2% Hit@5

**Файл для модификации**: `src/semantic_chunker.py` (можно добавить embedding-based chunking)

---

#### 5. `src/table_processor.py` - Query Enhancement
**Текущий подход**: Правила + словари  
**Возможные улучшения**:
- ✅ Использовать **LLM для query expansion** (опционально)
  - GPT-3.5/4 или локальная модель для умного расширения запросов
  - Генерация синонимов и реформулировок на основе контекста
  - Ожидаемый прирост: +1-3% Hit@5

**Файл для модификации**: `src/table_processor.py` (добавить функцию `enhance_query_with_llm`)

---

## 🔧 Что еще можно улучшить (не нейронки)

### 6. `src/retriever.py` - Улучшение RRF fusion
**Текущая реализация**: Базовая RRF через FlashRAG  
**Улучшения**:
- ✅ Кастомная реализация RRF с адаптивным k
- ✅ Учет разницы в диапазонах скоров dense и BM25
- ✅ Нормализация скоров перед fusion
- Ожидаемый прирост: +1-2% Hit@5

**Файл для модификации**: `src/retriever.py` (строки 213-290, метод `_build_router_config`)

---

### 7. `scripts/eval.py` и `scripts/submit.py` - Дедупликация
**Текущая реализация**: Улучшена (combined_score)  
**Дополнительные улучшения**:
- ✅ Учет разнообразия типов документов
- ✅ Boost для документов с таблицами для числовых запросов
- ✅ Penalty за дубликаты с очень похожим содержимым
- Ожидаемый прирост: +0.5-1% Hit@5

**Файлы для модификации**: `scripts/eval.py` (строки 365-423), `scripts/submit.py` (строки 312-370)

---

### 8. `src/text_processor.py` - Нормализация
**Текущая реализация**: Хорошая, но можно улучшить  
**Улучшения**:
- ✅ Лемматизация для русского языка (pymorphy2)
- ✅ Нормализация банковских терминов (счет/счёт → счёт)
- ✅ Обработка опечаток в числовых значениях
- Ожидаемый прирост: +0.5-1% Recall@K

**Файл для модификации**: `src/text_processor.py`

---

### 9. `src/evaluator.py` - Метрики
**Текущая реализация**: Базовые метрики  
**Улучшения**:
- ✅ Добавить nDCG@10, nDCG@20 для лучшей оценки качества
- ✅ Per-category метрики (запросы с числами, без чисел, и т.д.)
- ✅ Confidence intervals для метрик
- Ожидаемый прирост: Лучшая диагностика

**Файл для модификации**: `src/evaluator.py`

---

## 📊 Рекомендуемый порядок апгрейда

### Фаза 1: Быстрые победы (1-2 часа)
1. ✅ Включить ensemble reranker в `configs/models.yaml`
2. ✅ Обновить reranker на `BAAI/bge-reranker-v2-m3`
3. ✅ Включить second-pass reranking

**Ожидаемый прирост**: +4-7% Hit@5

### Фаза 2: Улучшение embeddings (2-4 часа)
1. ✅ Обновить на `BAAI/bge-m3` или `cohere multilingual-v3`
2. ✅ Пересобрать FAISS индекс
3. ✅ Оптимизировать batch sizes под новую модель

**Ожидаемый прирост**: +3-7% Hit@5

### Фаза 3: Дополнительные оптимизации (4-8 часов)
1. ✅ Улучшить RRF fusion
2. ✅ Добавить embedding-based semantic chunking
3. ✅ Улучшить нормализацию с лемматизацией

**Ожидаемый прирост**: +2-4% Hit@5

---

## 🎯 Конкретные рекомендации моделей

### Для максимального качества (если есть GPU 16GB+):
```yaml
embeddings:
  model_name: "BAAI/bge-m3"  # или "cohere-embed-multilingual-v3.0"
  batch_size: 32
  use_fp16: true

reranker:
  model_name: "BAAI/bge-reranker-v2-m3"
  max_length: 8192
  batch_size: 32
  use_fp16: true

reranker_ensemble:
  enabled: true
  models:
    - "BAAI/bge-reranker-v2-m3"
    - "BAAI/bge-reranker-base"
  weights: [0.7, 0.3]
  second_pass: true
  second_pass_topk: 20
```

### Для баланса качества и скорости (GPU 8GB):
```yaml
embeddings:
  model_name: "intfloat/multilingual-e5-large"  # уже используете
  batch_size: 48

reranker:
  model_name: "BAAI/bge-reranker-v2-m3"
  max_length: 512
  batch_size: 32

reranker_ensemble:
  enabled: true
  models:
    - "BAAI/bge-reranker-base"
    - "cross-encoder/ms-marco-MiniLM-L-12-v2"
  weights: [0.6, 0.4]
  second_pass: false
```

---

## 📝 Чеклист улучшений

- [ ] Фаза 1: Ensemble reranker + v2-m3
- [ ] Фаза 2: BGE-M3 embeddings
- [ ] Фаза 3: Улучшенный RRF fusion
- [ ] Фаза 4: Embedding-based chunking
- [ ] Фаза 5: Лемматизация
- [ ] Фаза 6: LLM query expansion (опционально)

---

## 🔗 Полезные ссылки

- BGE-M3: https://huggingface.co/BAAI/bge-m3
- BGE-Reranker-v2: https://huggingface.co/BAAI/bge-reranker-v2-m3
- Cohere Embeddings: https://cohere.com/embed
- PyMorphy2 (лемматизация): https://github.com/kmike/pymorphy2

---

**Примечание**: Все апгрейды требуют пересборки индексов (`make build_index`) и валидации на тестовых данных.

